# =============================================================================
# Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in the values.
# NEVER commit .env.local to version control.
# =============================================================================

# ------------------------------------------------------------------------------
# Application URL
# ------------------------------------------------------------------------------
# The canonical base URL of the application (without trailing slash).
# Used for SEO metadata, sitemaps, and OpenGraph tags.
#
# Examples:
#   - Local development: http://localhost:3000
#   - Production: https://www.jonathan-about.com
#
# IMPORTANT: This variable is prefixed with NEXT_PUBLIC_ and will be exposed
# to the browser. Do not include sensitive information.
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Google Tag Manager
# -----------------------------------------------------------------------------
# Your GTM container ID (format: GTM-XXXXXXXX)
# Get your container ID from https://tagmanager.google.com/
#
# Required: Yes (for analytics tracking)
# Example: GTM-ABC123X
# Note: NEXT_PUBLIC_ prefix exposes this to the browser (required for GTM)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_GTM_ID=

# -----------------------------------------------------------------------------
# CSP Extensions for Google Services (via GTM)
# -----------------------------------------------------------------------------
# These variables extend the Content Security Policy when additional Google
# services are configured in Google Tag Manager.
#
# Format: Space-separated list of domains (e.g., "https://example.com https://cdn.example.com")
# Reference: https://developers.google.com/tag-platform/security/guides/csp
#
# IMPORTANT: Only enable the services you actually use in GTM.
# Each variable adds domains to the corresponding CSP directive.
# Leave empty to disable (more restrictive CSP).
# -----------------------------------------------------------------------------

# CSP connect-src: Additional domains for fetch/XHR/WebSocket connections
NEXT_PUBLIC_CSP_CONNECT_SRC=

# CSP font-src: Additional domains for web fonts
NEXT_PUBLIC_CSP_FONT_SRC=

# CSP frame-src: Additional domains allowed to be embedded in iframes
NEXT_PUBLIC_CSP_FRAME_SRC=

# CSP img-src: Additional domains allowed for images/tracking pixels
NEXT_PUBLIC_CSP_IMG_SRC=

# CSP script-src: Additional domains allowed to execute scripts
NEXT_PUBLIC_CSP_SCRIPT_SRC=

# CSP style-src: Additional domains for stylesheets
NEXT_PUBLIC_CSP_STYLE_SRC=
